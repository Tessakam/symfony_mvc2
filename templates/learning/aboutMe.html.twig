{% extends 'base.html.twig' %}

{% block title %}Welcome, about me page{% endblock %}

{% block body %}
    <h1>Hello {{ name }}!</h1>
    <a href="{{ path('showMyName') }}">Previous page</a>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <input type="submit"  value="submit your name"/>
    {{ form_end(form) }}
{% endblock %}
{% extends 'base.html.twig' %}

{% block title %}Welcome, show name page{% endblock %}

{% block body %}
    <h1>Hello {{ name }}!</h1>
    <a href="{{ path('aboutMe') }}">About me</a>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <input type="submit"  value="submit your name"/>
    {{ form_end(form) }}
{% endblock %}